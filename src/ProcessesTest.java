/*
 * ProcessTest.java
 * CS 490 Team 3 Fall 2021
 * Test Process Performance functionality
 */
package src;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ProcessesTest {

    /*
     * Runs a process and ensures it runs properly without an interruption
     */
    /*@Test
    public void RunAProcess()
    {
        Processes processes = new Processes();
        assertTrue(processes.RunProcess("process a", 10, false));
    }\*

    /**
     * Runs a process and will cause an interruption. This test currently always fails
     */
    //@Test
    /*public void RunAnInterruptedProcess() throws InterruptedException {
        Processes processes = new Processes();
        assertFalse(processes.RunProcess("foo", 3, true));
    } */

}